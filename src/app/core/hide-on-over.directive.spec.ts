import { ElementRef } from '@angular/core';
import { EnfasizeOnOverDirective } from './hide-on-over.directive';

describe('EnfasizeOnOverDirective', () => {
  it('should create an instance', () => {
    //const directive = new EnfasizeOnOverDirective();
    //expect(directive).toBeTruthy();
  });
});
