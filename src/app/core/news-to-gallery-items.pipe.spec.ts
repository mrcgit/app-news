import { NewsToGalleryItemsPipe } from './news-to-gallery-items.pipe';

describe('NewsToGalleryItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new NewsToGalleryItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
