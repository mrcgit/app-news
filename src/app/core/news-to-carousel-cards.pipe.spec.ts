import { NewsToCarouselCardsPipe } from './news-to-carousel-cards.pipe';

describe('NewsToCarouselCardsPipe', () => {
  it('create an instance', () => {
    const pipe = new NewsToCarouselCardsPipe();
    expect(pipe).toBeTruthy();
  });
});
